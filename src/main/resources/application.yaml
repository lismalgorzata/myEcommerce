server:
  port: 8181

spring:
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password: ~
    h2:
        console:
            enabled: true
            path: /h2

jpa:
  database-platform: org.hibernate.dialect.H2Dialect
